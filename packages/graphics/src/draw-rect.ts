import { Camera, Viewport } from '@sim-v3/core'
import { Context } from './context.js'

export function drawRect(
  context: Context,
  camera: Camera,
  viewport: Viewport,
  x: number,
  y: number,
  w: number,
  h: number,
): void {}
